import React from 'react';
import { Theme, Layout } from './components/core';

const App = () => {
    return (
        <Theme>
            <Layout></Layout>
        </Theme>
    );
};

export default App;
